# Tasks

## 1. Executive Bento Profile & Telemetry Optimization

- [x] 1.1 Update `pages/profile.vue` daily pace chips from `grid-cols-4` to `grid-cols-2 sm:grid-cols-4 gap-2`
- [x] 1.2 Audit `EngineerIdentityPassport.vue` and `EngineerMilestonesCard.vue` for responsive wrapping on 320px screens
- [x] 1.3 Ensure `DomainGoalTracker.vue` pillar cards stack cleanly with fluid progress bar animations

## 2. Settings Web Push & Timezone Controls

- [x] 2.1 Audit `pages/settings.vue` for `AppTimePicker` integrations, timezone selector popovers, and mobile container boundaries
- [x] 2.2 Verify Brave browser web push setup guidance card layout on small mobile displays

## 3. Login Authentication & Form Layout

- [x] 3.1 Audit `pages/login.vue` for Google GIS button responsive width and zero-shift tab border transitions
- [x] 3.2 Verify RFC 7807 error problem details display cleanly with accessible alerts
- [x] 3.3 Verify unit test suite passes for profile, settings, and login pages via `npm test`
