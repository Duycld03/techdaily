# Tasks: Frontend UI Phase 5 - Global Shell, Navigation & Authentication

## 1. Frontend - Global Navigation Chrome & Header Elevation

- [x] 1.1 Standardize sticky header elevation, compact padding, and telemetry controls in `frontend/components/layout/AppHeader.vue`
- [x] 1.2 Polish desktop sidebar active state indicators, semantic grouping, and label nowrap in `frontend/components/layout/AppSidebar.vue`
- [x] 1.3 Ensure mobile navigation slide-over drawer adheres to `z-50` elevation, body scroll lock, and touch gesture safety

## 2. Frontend - Authentication Surface & Viewport Bounding
- [x] 2.1 Refine centered authentication card geometry, glassmorphic styling, and viewport bounding in `frontend/pages/login.vue`
- [x] 2.2 Eliminate layout shifting during login vs registration mode toggling in `frontend/pages/login.vue`

## 3. Frontend - MasterDetailLayout & Settings Standardization
- [x] 3.1 Polish sub-navigation rail and responsive content panel in `frontend/components/layout/MasterDetailLayout.vue`
- [x] 3.2 Standardize multi-column settings sections and responsive form layouts in `frontend/pages/settings.vue`

## 4. Testing & Verification
- [x] 4.1 Update layout, login, and settings unit tests in `frontend/tests/` to verify responsive chrome and master-detail behavior
- [x] 4.2 Execute full frontend test suite (`npm test`) and run production build (`npm run build`) to ensure zero regressions
