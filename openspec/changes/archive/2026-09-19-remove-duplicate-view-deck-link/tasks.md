# Tasks

## 1. Frontend (UI Decluttering)

- [x] 1.1 Remove redundant header `<NuxtLink to="/review">` element from `frontend/components/today/ConcentricMetricCard.vue`.
- [x] 1.2 Update unit tests in `frontend/tests/components/today/ConcentricMetricCard.spec.ts` to assert that exactly one review deck navigation element exists in the component.

## 2. Verification & Testing

- [x] 2.1 Run component tests (`npx vitest run tests/components/today/ConcentricMetricCard.spec.ts`) and verify passing assertions.
- [x] 2.2 Run full frontend test suite (`npm test`) and verify 100% passing tests.
