# Tasks

## 1. Frontend — Command Palette Input

- [x] 1.1 Add `focus-visible:outline-none focus-visible:shadow-none` to the search input class in `AppCommandPalette.vue` (line 249) to suppress the global focus-visible ring on programmatic focus

## 2. Verification

- [x] 2.1 Open Command Palette (⌘K / Ctrl+K) and confirm no purple focus ring appears on the search input
